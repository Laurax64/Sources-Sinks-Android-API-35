plugins {
    kotlin("js") version "1.9.0" // Use the latest Kotlin version
}

repositories {
    mavenCentral()
}

dependencies {
    implementation(kotlin("stdlib-js"))
}

kotlin {
    js(IR) {
        browser {
            // Multiple outputs configuration
            webpackTask {
                output.filename = "[name].js" // Each file will have a separate JS output
            }
        }
    }
}
